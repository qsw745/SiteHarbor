DATABASE_URL="file:../data/siteharbor.db"
SESSION_SECRET="replace_with_at_least_32_random_characters"
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Optional first-run seed for older deployments. Prefer reset-admin-password.
# ADMIN_PASSWORD_HASH='$2b$12$replace_with_bcrypt_hash_generated_by_npm_run_hash_password'
